# Practice

1. Измените `BodyDiameter` и `BodyHeight`.
2. Переключите `MountType` между `Ceiling` и `Wall`.
3. Переключите `IndicatorMode` между `BuiltIn` и `Remote`.
4. Измените `PortLayout` и `TerminalOffset`.
5. Добавьте в 2D-символ еще одну внутреннюю окружность.

