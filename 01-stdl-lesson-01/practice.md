# Practice

1. Поменяйте `defaultName` и пересоберите шаблон.
2. Увеличьте `Width` и сравните новую форму со старой.
3. Увеличьте `Height` и проверьте, как поднялся 2D-символ.
4. Добавьте второй прямоугольник в `main.lua` внутри `makeTopViewSymbol()`.
5. Временно замените `CreateBlock(width, depth, height)` на `CreateBlock(width, depth, height / 2)`.
