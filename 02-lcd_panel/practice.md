# Practice

1. Измените `PanelWidth`, `PanelHeight` и `PanelDepth`.
2. Поменяйте `ScreenMargin` и посмотрите, как меняется экран.
3. Переключите `MountType` между `Wall` и `Table`.
4. При `Table` измените `StandDepth`.
5. Добавьте в символ панели кнопку или маленький круг внизу экрана.

