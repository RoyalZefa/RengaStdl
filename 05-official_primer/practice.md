# Practice

1. Поменяйте `bodyLength`, `bodyWidth`, `bodyHeight`.
2. Измените точки в `makeProfile()` и пересоберите корпус.
3. Уберите символ `letterS` и оставьте только контур.
4. Поиграйте со `connectionSide` и `connectionDirection` у трубных групп.
5. Проверьте, как скрываются `threadSize` и `nominalDiameter`.

